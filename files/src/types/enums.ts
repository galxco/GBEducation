// =============================================================
//  GBEducation — Enums compartilhados
//  Espelha os enums do schema Prisma para uso no código
//  antes de rodar `prisma generate`
// =============================================================

export enum TipoUsuario {
  ALUNO = "ALUNO",
  DOCENTE = "DOCENTE",
  ADMIN = "ADMIN",
}
